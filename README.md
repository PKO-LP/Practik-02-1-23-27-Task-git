**Пошаговая иснтрукция: от локального репозитория к удаленному (по умолчанию ветка будет `main`, как на GitHub)**  


### 1. Сначала на сайте GitHub (один раз)
1. Зайди на **github.com** → нажми зелёную кнопку **New repository**.
2. Придумай название проекта.
3. **НЕ ставь** галочки на README, .gitignore и license (репозиторий должен быть пустым).
4. Нажми **Create repository**.
5. Скопируй HTTPS-ссылку (кнопка **Code** → HTTPS).  
   Пример: `https://github.com/твой_логин/мой_проект.git`

### 2. Создай Personal Access Token (нужен вместо пароля)
1. На GitHub: аватарка → **Settings** → слева внизу **Developer settings** → **Personal access tokens** → **Tokens (classic)**.
2. Нажми **Generate new token (classic)**.
3. Поставь галочку **только "repo"**.
4. Скопируй токен (начинается на `ghp_` или `github_pat_`).  
   **Сохрани его где-нибудь** — он больше не покажется!

### 3. В терминале (открой папку с твоим проектом)
По очереди копируй и вставляй эти команды:

```bash
git init                  # создаём локальный git-репозиторий
git add .                 # добавляем ВСЕ файлы проекта
git commit -m "Initial commit"   # делаем первый коммит

git branch -M main        # переименовываем ветку в main (чтобы не было проблем!)

git remote add origin https://oauth2:ЗДЕСЬ_ВСТАВЬТЕ_СВОЙ_ТОКЕН@github.com/пожалуйста_не_копируйте_бездумно_данную_строчку_так_как_здесь_должен_быть_Ваш_логин/а_здесь_название_репо.git
                          # подключаем твой репозиторий на GitHub

git push -u origin main   # заливаем проект на GitHub
```

**При `git push` тебя спросит:**
- **Username:** введи свой логин GitHub
- **Password:** вставь **токен** (тот длинный, который ты сгенерировал)

Готово!  
Через пару секунд твой проект появится на GitHub.

Если хочешь **ещё проще** (без ввода токена каждый раз) — напиши, дам вариант с GitHub CLI.  
Удачи! 🚀
